diff --git a/AndroidCompat/src/main/java/android/text/Html.java b/AndroidCompat/src/main/java/android/text/Html.java
index 443c8b27..4bf8de88 100644
--- a/AndroidCompat/src/main/java/android/text/Html.java
+++ b/AndroidCompat/src/main/java/android/text/Html.java
@@ -17,10 +17,16 @@ import org.xml.sax.XMLReader;
 
 public class Html {
 
+    public static final int FROM_HTML_MODE_LEGACY = 0;
+
     public static Spanned fromHtml(String source) {
         return new FakeSpanned(Jsoup.clean(source, Safelist.none()));
     }
 
+    public static Spanned fromHtml(String source, int flags) {
+        return fromHtml(source);
+    }
+
     public static Spanned fromHtml(String source, Html.ImageGetter imageGetter, Html.TagHandler tagHandler) {
         throw new RuntimeException("Stub!");
     }
diff --git a/AndroidCompat/src/main/java/xyz/nulldev/androidcompat/androidimpl/CustomContext.java b/AndroidCompat/src/main/java/xyz/nulldev/androidcompat/androidimpl/CustomContext.java
index 2962a830..3dfb5f15 100644
--- a/AndroidCompat/src/main/java/xyz/nulldev/androidcompat/androidimpl/CustomContext.java
+++ b/AndroidCompat/src/main/java/xyz/nulldev/androidcompat/androidimpl/CustomContext.java
@@ -17,6 +17,7 @@
 package xyz.nulldev.androidcompat.androidimpl;
 
 import android.content.*;
+import android.app.ActivityManager;
 import android.content.pm.ApplicationInfo;
 import android.content.pm.PackageManager;
 import android.content.res.AssetManager;
@@ -80,6 +81,7 @@ public class CustomContext extends Context {
 
     private Map<String, Object> serviceMap = new HashMap<>();
     {
+        serviceMap.put(Context.ACTIVITY_SERVICE, createActivityManager());
         serviceMap.put(Context.CONNECTIVITY_SERVICE, ConnectivityManager.INSTANCE);
         serviceMap.put(Context.POWER_SERVICE, PowerManager.INSTANCE);
     }
@@ -106,7 +108,7 @@ public class CustomContext extends Context {
 
     @Override
     public Looper getMainLooper() {
-        return null;
+        return Looper.getMainLooper();
     }
 
     @Override
@@ -115,6 +117,14 @@ public class CustomContext extends Context {
         return this;
     }
 
+    private static Object createActivityManager() {
+        try {
+            return ActivityManager.class.getDeclaredConstructor().newInstance();
+        } catch (ReflectiveOperationException error) {
+            throw new IllegalStateException("Unable to create ActivityManager", error);
+        }
+    }
+
     @Override
     public void setTheme(int i) {
 
