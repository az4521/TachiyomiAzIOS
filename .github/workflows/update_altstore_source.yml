name: Republish AltStore Source

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: GitHub release tag containing the IPA
        required: true
        default: nightly

permissions:
  contents: write

jobs:
  update-source:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Generate source
        run: >-
          python3 .github/workflows/supporting/update_altstore_json.py
          --tag "${{ inputs.release_tag }}"

      - name: Validate source
        run: python3 -m json.tool .github/workflows/supporting/altstore/apps.json >/dev/null

      - name: Deploy source to altstore branch
        uses: JamesIves/github-pages-deploy-action@v4.7.2
        with:
          branch: altstore
          folder: .github/workflows/supporting/altstore
          git-config-name: github-actions[bot]
          git-config-email: github-actions[bot]@users.noreply.github.com
          commit-message: Update TachiyomiAZ AltStore source
          single-commit: true
